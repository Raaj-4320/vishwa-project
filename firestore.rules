rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // WARNING: Authentication has been removed. 
    // These rules allow public access for development purposes.
    // In a production app, you should implement proper authorization.

    match /{document=**} {
      allow read, write: if true;
    }
  }
}
